# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: Default

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'MSEC_Candidates_Homeworks(a8108c2b-496c-424d-8347-ecc8afb6384c)'
    subscriptionId: 'a8108c2b-496c-424d-8347-ecc8afb6384c'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'JARMPipeline-rg'
    location: 'westeurope'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://raw.githubusercontent.com/juna-an/MSInterviewIntro/main/azureSATemplate.json'
    overrideParameters: '-projectName myarmpipeline'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
